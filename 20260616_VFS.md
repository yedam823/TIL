리눅스를 사용하면 다양한 포맷디스크를 사용할 수 있다.

보통 리눅스는 ext3, ext4 등을 사용하지만 윈도우에서 쓰는 NTFS나 FAT도 쓸수있는거 아닌가!

근데 이런걸 모두 open, close같은 공통된 인터페이스로 접근한다.

이렇게 할 수 있게 만드는 계층을 VFS라고 한다.

### 같이 알아두면 좋은 개념

- inode : 이름 제외 파일의 정보들을 담는 구조체
- dentry : 파일 이름이 없는 inode와 파일 이름을 연결해줌. 개빠르게(캐시로)
- super_block : 파일 시스템의 전체 정보를 담음.

→ superblock 안에서 dentry(이름) ↔ inode(정보) ↔ datablock(데이터)
