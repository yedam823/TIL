## 레지스터 구조

메모리 계층 구조의 최상위에 위치한 매우 빠른 기억장치

레지스터의 비트 크기 = WORD 크기

## 동작과정

CPU의 기능을 수행하기 위해 사용됨

1. Fetch : 사용자 명령과 주 기억장치에 저장된 프로그램을 CPU 프로세서에 가져온다
2. Decode : 가져온 명령을 해석한다
3. Execute : CPU에 의해 생성된 결과를 저장한다

## 종류와 특징

다양한 용도로 사용되며 각 특정 기능을 수행한다

- 메모리 주소 레지스터(MAR) : CPU가 데이터를 읽거나 쓰려는 메모리 주소를 일시적으로 저장
- 메모리 버퍼 레지스터(MBR) : 메모리에 읽거나 쓰려는 데이터 혹은 명령을 일시적으로 저장(명령 - IR, 데이터 - AC, I/O)
- I/O 주소 레지스터(I/O AR) : 특정 I/O 입출력 장치의 주소를 지정하는데 사용된다
- I/O 버퍼 레지스터(I/O BR) : I/O 모듈과 프로세서 간에 데이터를 교환하는 데 사용된다
- PC : 프로세서가 수행할 다음 명령의 주소를 일시적으로 저장하는데 사용된다
- IR : 주기억장치에서 수행할 명령을 가져오면 명령어 레지스터에 저장됨. 제어장치는 이 레지스터의 지시를 받아 실행
- 누산기 레지스터(AC) : ALU의 산술 및 논리 연산 중에 사용됨. 제어장치는 산술 또는 논리 동작을 위해 주기억장치에서 가져온 데이터를 AC에 저장함. → 시스템 사용될 초기 데이터, 중간 결과 및 최종 결과를 보관
- 스택 제어 레지스터(SCR) : 스택 is 메모리 블록의 집합. 이 스택을 FILO로 관리함. 이 레지스터의 크기는 2~4바이트임
- 플래그 레지스터(FR) : CPU 작동중 특정 상태가 발생했음을 나타내는 데 사용. 크기는 1~2바이트인 특수 목적 레지스터
- 데이터 레지스터(DR) : MCU에서 주변 페리페럴 장치들로 전달되는 데이터를 임시 저장함
